const initialSate = {
    posts: [],
    post: {},
    loading: false
};

export default function(state = initialSate, action) {
    switch(action.type){
        default:
        return state;
    }
}